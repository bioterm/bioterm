# coding: utf-8

"""
    Grafana HTTP API.

    The Grafana backend exposes an HTTP API, the same API is used by the frontend to do everything from saving dashboards, creating users and updating data sources.

    The version of the OpenAPI document: 0.0.1
    Contact: hello@grafana.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictBool, StrictInt, StrictStr
from pydantic import Field
from grafanaApiClient.models.correlation_config import CorrelationConfig
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class Correlation(BaseModel):
    """
    Correlation is the model for correlations definitions
    """ # noqa: E501
    config: Optional[CorrelationConfig] = None
    description: Optional[StrictStr] = Field(default=None, description="Description of the correlation")
    label: Optional[StrictStr] = Field(default=None, description="Label identifying the correlation")
    org_id: Optional[StrictInt] = Field(default=None, description="OrgID of the data source the correlation originates from", alias="orgId")
    provisioned: Optional[StrictBool] = Field(default=None, description="Provisioned True if the correlation was created during provisioning")
    source_uid: Optional[StrictStr] = Field(default=None, description="UID of the data source the correlation originates from", alias="sourceUID")
    target_uid: Optional[StrictStr] = Field(default=None, description="UID of the data source the correlation points to", alias="targetUID")
    uid: Optional[StrictStr] = Field(default=None, description="Unique identifier of the correlation")
    __properties: ClassVar[List[str]] = ["config", "description", "label", "orgId", "provisioned", "sourceUID", "targetUID", "uid"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of Correlation from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of config
        if self.config:
            _dict['config'] = self.config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of Correlation from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "config": CorrelationConfig.from_dict(obj.get("config")) if obj.get("config") is not None else None,
            "description": obj.get("description"),
            "label": obj.get("label"),
            "orgId": obj.get("orgId"),
            "provisioned": obj.get("provisioned"),
            "sourceUID": obj.get("sourceUID"),
            "targetUID": obj.get("targetUID"),
            "uid": obj.get("uid")
        })
        return _obj


