# coding: utf-8

"""
    Grafana HTTP API.

    The Grafana backend exposes an HTTP API, the same API is used by the frontend to do everything from saving dashboards, creating users and updating data sources.

    The version of the OpenAPI document: 0.0.1
    Contact: hello@grafana.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictBool, StrictInt, StrictStr, field_validator
from pydantic import Field
from typing_extensions import Annotated
from grafanaApiClient.models.alert_query import AlertQuery
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class ProvisionedAlertRule(BaseModel):
    """
    ProvisionedAlertRule
    """ # noqa: E501
    annotations: Optional[Dict[str, StrictStr]] = None
    condition: StrictStr
    data: List[AlertQuery]
    exec_err_state: StrictStr = Field(alias="execErrState")
    folder_uid: StrictStr = Field(alias="folderUID")
    var_for: StrictInt = Field(description="A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.", alias="for")
    id: Optional[StrictInt] = None
    is_paused: Optional[StrictBool] = Field(default=None, alias="isPaused")
    labels: Optional[Dict[str, StrictStr]] = None
    no_data_state: StrictStr = Field(alias="noDataState")
    org_id: StrictInt = Field(alias="orgID")
    provenance: Optional[StrictStr] = None
    rule_group: Annotated[str, Field(min_length=1, strict=True, max_length=190)] = Field(alias="ruleGroup")
    title: Annotated[str, Field(min_length=1, strict=True, max_length=190)]
    uid: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=40)]] = None
    updated: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["annotations", "condition", "data", "execErrState", "folderUID", "for", "id", "isPaused", "labels", "noDataState", "orgID", "provenance", "ruleGroup", "title", "uid", "updated"]

    @field_validator('exec_err_state')
    def exec_err_state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('OK', 'Alerting', 'Error'):
            raise ValueError("must be one of enum values ('OK', 'Alerting', 'Error')")
        return value

    @field_validator('no_data_state')
    def no_data_state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('Alerting', 'NoData', 'OK'):
            raise ValueError("must be one of enum values ('Alerting', 'NoData', 'OK')")
        return value

    @field_validator('uid')
    def uid_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z0-9-_]+$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z0-9-_]+$/")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of ProvisionedAlertRule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
                "updated",
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in data (list)
        _items = []
        if self.data:
            for _item in self.data:
                if _item:
                    _items.append(_item.to_dict())
            _dict['data'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of ProvisionedAlertRule from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "annotations": obj.get("annotations"),
            "condition": obj.get("condition"),
            "data": [AlertQuery.from_dict(_item) for _item in obj.get("data")] if obj.get("data") is not None else None,
            "execErrState": obj.get("execErrState"),
            "folderUID": obj.get("folderUID"),
            "for": obj.get("for"),
            "id": obj.get("id"),
            "isPaused": obj.get("isPaused"),
            "labels": obj.get("labels"),
            "noDataState": obj.get("noDataState"),
            "orgID": obj.get("orgID"),
            "provenance": obj.get("provenance"),
            "ruleGroup": obj.get("ruleGroup"),
            "title": obj.get("title"),
            "uid": obj.get("uid"),
            "updated": obj.get("updated")
        })
        return _obj


