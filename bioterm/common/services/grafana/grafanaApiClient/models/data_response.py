# coding: utf-8

"""
    Grafana HTTP API.

    The Grafana backend exposes an HTTP API, the same API is used by the frontend to do everything from saving dashboards, creating users and updating data sources.

    The version of the OpenAPI document: 0.0.1
    Contact: hello@grafana.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictInt, StrictStr
from pydantic import Field
from grafanaApiClient.models.frame import Frame
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class DataResponse(BaseModel):
    """
    A map of RefIDs (unique query identifiers) to this type makes up the Responses property of a QueryDataResponse. The Error property is used to allow for partial success responses from the containing QueryDataResponse.
    """ # noqa: E501
    error: Optional[StrictStr] = Field(default=None, description="Error is a property to be set if the corresponding DataQuery has an error.", alias="Error")
    error_source: Optional[StrictStr] = Field(default=None, description="ErrorSource type defines the source of the error", alias="ErrorSource")
    frames: Optional[List[Frame]] = Field(default=None, description="It is the main data container within a backend.DataResponse. There should be no `nil` entries in the Frames slice (making them pointers was a mistake).", alias="Frames")
    status: Optional[StrictInt] = Field(default=None, alias="Status")
    __properties: ClassVar[List[str]] = ["Error", "ErrorSource", "Frames", "Status"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of DataResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in frames (list)
        _items = []
        if self.frames:
            for _item in self.frames:
                if _item:
                    _items.append(_item.to_dict())
            _dict['Frames'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of DataResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "Error": obj.get("Error"),
            "ErrorSource": obj.get("ErrorSource"),
            "Frames": [Frame.from_dict(_item) for _item in obj.get("Frames")] if obj.get("Frames") is not None else None,
            "Status": obj.get("Status")
        })
        return _obj


