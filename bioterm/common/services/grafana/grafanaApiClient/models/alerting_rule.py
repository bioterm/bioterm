# coding: utf-8

"""
    Grafana HTTP API.

    The Grafana backend exposes an HTTP API, the same API is used by the frontend to do everything from saving dashboards, creating users and updating data sources.

    The version of the OpenAPI document: 0.0.1
    Contact: hello@grafana.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from typing import Any, ClassVar, Dict, List, Optional, Union
from pydantic import BaseModel, StrictFloat, StrictInt, StrictStr
from pydantic import Field
from grafanaApiClient.models.alert import Alert
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class AlertingRule(BaseModel):
    """
    adapted from cortex
    """ # noqa: E501
    active_at: datetime = Field(alias="activeAt")
    alerts: Optional[List[Alert]] = None
    annotations: Dict[str, StrictStr] = Field(description="The custom marshaling for labels.Labels ends up doing this anyways.")
    duration: Optional[Union[StrictFloat, StrictInt]] = None
    evaluation_time: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="evaluationTime")
    health: StrictStr
    labels: Optional[Dict[str, StrictStr]] = Field(default=None, description="The custom marshaling for labels.Labels ends up doing this anyways.")
    last_error: Optional[StrictStr] = Field(default=None, alias="lastError")
    last_evaluation: Optional[datetime] = Field(default=None, alias="lastEvaluation")
    name: StrictStr
    query: StrictStr
    state: StrictStr = Field(description="State can be \"pending\", \"firing\", \"inactive\".")
    totals: Optional[Dict[str, StrictInt]] = None
    totals_filtered: Optional[Dict[str, StrictInt]] = Field(default=None, alias="totalsFiltered")
    type: StrictStr
    __properties: ClassVar[List[str]] = ["activeAt", "alerts", "annotations", "duration", "evaluationTime", "health", "labels", "lastError", "lastEvaluation", "name", "query", "state", "totals", "totalsFiltered", "type"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of AlertingRule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in alerts (list)
        _items = []
        if self.alerts:
            for _item in self.alerts:
                if _item:
                    _items.append(_item.to_dict())
            _dict['alerts'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of AlertingRule from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "activeAt": obj.get("activeAt"),
            "alerts": [Alert.from_dict(_item) for _item in obj.get("alerts")] if obj.get("alerts") is not None else None,
            "annotations": obj.get("annotations"),
            "duration": obj.get("duration"),
            "evaluationTime": obj.get("evaluationTime"),
            "health": obj.get("health"),
            "labels": obj.get("labels"),
            "lastError": obj.get("lastError"),
            "lastEvaluation": obj.get("lastEvaluation"),
            "name": obj.get("name"),
            "query": obj.get("query"),
            "state": obj.get("state"),
            "totals": obj.get("totals"),
            "totalsFiltered": obj.get("totalsFiltered"),
            "type": obj.get("type")
        })
        return _obj


